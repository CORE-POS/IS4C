<?php

header('Location: AjaxPollScale.php');
/*
ini_set('display_errors','Off');
include_once(dirname(__FILE__).'/../lib/AutoLoader.php');

$scaleDriver = CoreLocal::get("scaleDriver");
$sd = 0;
if ($scaleDriver != "")
    $sd = new $scaleDriver();

if (is_object($sd))
    $sd->ReadFromScale();    
else
    echo "{}"; // no driver => empty json
*/

